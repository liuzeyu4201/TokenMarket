# Converge: 031-project-proxy-key-scope

**Date**: 2026-08-31

实现满足 spec.md、plan.md 与 tasks.md：

- V0.2 Key 归属 Project，权限 ⊆ Binding。
- 明文只在签发/轮换响应；存储 HMAC；列表仅前后缀。
- 协议/模型/CIDR/过期/周期额度取交集；并发额度不超发。
- 撤销 ≤1s（Postgres 直读 + 网关正向缓存 TTL 1s）；已撤销不可启用。
- 跨 Project 同形 404。volcano 历史路径保留。

领域包覆盖率 ≥80%。

Converged — the implementation satisfies the spec, plan, and tasks.
