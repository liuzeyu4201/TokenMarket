# 功能规格：代理 Key 认证

**功能分支**: `011-proxy-key-auth`
**创建日期**: 2026-08-24
**状态**: 已规划
**源规格**: `项目开发/V0.1/V0.1_0712/specs/SF11-*.md`（SF11）

## 目标与价值

网关 Bearer 认证代理 Key，pepper HMAC 查找，失败 401 且不调上游。

## 用户场景与测试

### 用户故事 1 - 主路径 (优先级: P1)

作为调用方，我希望本功能交付源规格中的主路径并可独立验收。

**独立测试**: 领域单测覆盖主路径与负向（授权/幂等/分类/脱敏）。

**验收场景**:

1. **Given** 合法输入，**When** 执行主用例，**Then** 行为符合源 SF11 功能需求。
2. **Given** 负向输入，**When** 执行，**Then** 稳定错误码且不泄漏秘密。

### 边界情况

- 源规格 Edge Cases 均纳入测试意图；V0.1 仅 volcano Chat Completions。

## 需求

### 功能需求

- **FR-001**: 实现源规格 SF11 的范围内行为，边界遵守宪章 II/III/V。
- **FR-002**: 秘密不得出现在响应、日志、测试快照。
- **FR-003**: 幂等或并发若适用必须有负向测试。

### 工程需求

- **ER-001 — 契约**: 版本化契约或领域端口先于消费者。
- **ER-002 — 安全**: 认证加密或不可逆哈希按对象分类。
- **ER-003 — 数据**: PostgreSQL 为持久事实源（若本功能持久化）。
- **ER-004 — 性能**: 有界超时；生成不默认重试。
- **ER-005 — 可靠性**: 失败关闭，无半完成可路由状态。
- **ER-006 — 可观测**: request_id 与低基数指标。
- **ER-007 — 可访问性**: V0.1 无前端。

### 失败与恢复

1. **Given** 依赖失败，**When** 操作，**Then** 有界结束且无秘密泄漏。

### 关键实体

- 见源规格 SF11 Key Entities；实现落点 `services/proxy-gateway/internal/domain/proxyauth/`。

## 成功标准

- **SC-001**: 源规格主路径自动化测试通过。
- **SC-002**: 负向/脱敏测试通过。
- **SC-003**: 变更领域包覆盖率目标 80%。

## 范围

范围内：SF11 In Scope。范围外：源 Out of Scope（多平台、资金、Kafka 强制计量）。

## 假设

依赖按 `项目开发/V0.1/V0.1_0712/specs/README.md` 表格。

## Clarifications

### Session 2026-08-24

- Q: 分析/实现策略？ → A: 采用源规格最完整在范围内方案；SF08 在 SF06 `quota_unavailable` 时允许持久化但 health=unknown（不可路由直至成功额度/健康）。
