# Converge: 030-provider-binding

**Date**: 2026-08-31

实现满足 spec.md、plan.md 与 tasks.md：

- 三协议可同时 active；SDK 提示无凭据。
- 并发发布后 active 行数为 1；旧 version 行不变。
- mode 不一致与跨协议准入拒绝。
- dedicated Connection 失效 → degraded，无共享池回退。
- 发布后 SF10 启用协议通过；未发布仍 `PROVIDER_BINDING_REQUIRED`。

领域包覆盖率 ≥80%。Connection 实体与费率表仍由后续 SF 落地。
