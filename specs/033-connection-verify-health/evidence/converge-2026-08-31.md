# Converge: 033-connection-verify-health

**Date**: 2026-08-31

T001–T011 complete.

- Contract `provider-connection/v1` 1.2.0: verify, health, capabilities, internal health.
- Alembic `0015_connection_health` (head assertion updated).
- VendorProbe port; default fail-closed; ScriptedProbe in tests (no paid vendor calls).
- Six result categories; catalog ∩ snapshot excludes control_plane/beta/unknown paths.
- Health hysteresis: single upstream_fault from healthy → degraded; fail threshold 3 → unhealthy.
- Manual re-verify immediate recovery; scheduled uses thresholds.
- Probe tick budget default 8; 1000 due connections one tick ≤ budget.
- Public diagnostics/logs/audit contain no secret.

**Coverage**: `app.domain.connections` + `app.api.v1.connections` = 94% (≥80%).

**Migration head**: `0015_connection_health`.

Converged — the implementation satisfies the spec, plan, and tasks.
