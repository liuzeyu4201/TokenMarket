# shared/

Versioned repository contracts and shared validation tooling.

## Contents

- `contracts/` — cross-component contract source of truth
- `tools/` — reproducible contract validation helpers
- `tests/` — contract drift and bundle tests

## Build output

`make build` produces a deterministic tar.gz archive of the contract assets.
