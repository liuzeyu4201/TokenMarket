# Workflow Test Fixtures

This directory holds synthetic fixtures for repository workflow tests.

All fixtures are deliberately synthetic and contain no real credentials,
production addresses, or personal data.
