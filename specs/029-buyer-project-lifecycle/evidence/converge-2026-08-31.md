# Converge: 029-buyer-project-lifecycle

**Date**: 2026-08-31

实现满足 spec.md、plan.md 与 tasks.md：

- `project/v1` 1.1.0 expand-only；PATCH 无 mode。
- Alembic `0011_buyer_projects` 含 mode 触发器、账号内名称部分唯一、阻塞表。
- 创建后启用协议经 `EmptyBindingLookup` 失败关闭。
- 归档后准入直读 `allows_new_proxy=false`。
- IDOR 同形 `NOT_FOUND`；卖家工作区 403。
- 买家 UI `/projects` 使用设计系统组件并展示模式后果。

领域包覆盖率 ≥80%。未实现 Binding/Key/账本实体（后续 SF）。

Converged — the implementation satisfies the spec, plan, and tasks.
