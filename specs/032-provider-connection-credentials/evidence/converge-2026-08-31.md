# Converge: 032-provider-connection-credentials

**Date**: 2026-08-31

T001–T012 complete.

- Contract `provider-connection/v1` 1.1.0: list/get/put credential/delete/internal unwrap; writeOnly secret.
- Alembic `0014_provider_connections` (head assertion updated).
- Envelope encryption via `CredentialEncryptor`; fingerprint HMAC; public JSON never contains secret/ciphertext.
- Internal unwrap requires `X-Internal-Token`; previous key_version decrypts.
- SSRF: HTTPS only; loopback/RFC1918/link-local/metadata/userinfo/redirect rejected.
- Credential replace CAS (`credential_version`); concurrent mix = 0.
- Delete wipes nonce/ciphertext/tag; unwrap 404; fingerprint retained in audit; dedicated Binding `degraded` without shared fallback.
- Authz `connection.*` seller/both only; buyer workspace 403.
- UI `/connections` password field; post-submit value cleared; buyer forbidden.

**Tests**

- `tests/unit/test_connection_ssrf.py`
- `tests/unit/test_connection_credentials.py`
- `tests/unit/test_connection_http.py`
- `frontend/src/pages/Connections.test.tsx`

**Coverage**: `app.domain.connections` + `app.api.v1.connections` = 95% (≥80%).

**Migration head**: `0014_provider_connections`.
